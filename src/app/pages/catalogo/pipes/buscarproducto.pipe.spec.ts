import { BuscarproductoPipe } from './buscarproducto.pipe';

describe('BuscarproductoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarproductoPipe();
    expect(pipe).toBeTruthy();
  });
});
